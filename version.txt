1.1.2.1~;~\n\t* Fixed a bug that caused errors and strange formatting when adding spaces to multi-select inputs.\n\t* Added a check to prevent a bug that would send 2 nui responses causing errors.\nThis update did NOT change the config file. However we do reccomend backing up your config before updating.
